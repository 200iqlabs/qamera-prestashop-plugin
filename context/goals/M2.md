# /goal — M2 Zakładka produktu + odczyt stanu z API

> Gotowa komenda `/goal`. Świeża sesja. Kontekst: `goals.md` → M2. Ewaluator czyta TYLKO transkrypt.
> Wymaga: M1 ukończony.

## Komenda

```
/goal The product-tab UI and API-backed state reading for the qameraai module are implemented under qameraai/. End state, all present and valid: qameraai/qameraai.php registers the displayAdminProductsExtra hook (and the PS9 equivalent) rendering a Smarty template and enqueues admin JS/CSS on the product edit screen; qameraai/views/templates/hook/product-tab.tpl renders the generator UI (left: upload/role + session settings; right: containers grouping a photo -> its packshots -> their sessions); qameraai/views/js/qamera-product.js and qameraai/views/css/qamera-admin.css exist; qameraai/classes/QameraApiClient.php gains get_product(external_ref) [GET /products/{ref} with embedded images+packshots], list_jobs(), get_models/get_sceneries/get_ai_models, and a 15-min catalog cache via qameraai/classes/QameraCatalogCache.php; the render derives roles+approval from the API (ProductPackshot.voting accepted/pending and job voting) and lineage (source_image_id, packshot_asset_id), with explicit empty-state and catalog-error handling. PHP 7.4-compatible syntax, PrestaShop 8 and 9. Prove completion in your final message by: (1) listing every created/modified file path under qameraai/, and (2) pasting `php -l` output showing "No syntax errors detected" for each .php file (if php CLI is unavailable, state that and instead quote the new QameraApiClient method signatures and the grouping/voting-mapping block from the tpl/php). Constraints: only files under qameraai/ are created or modified; prd.md, goals.md, CLAUDE.md and context/ are NOT touched; no libraries beyond PrestaShop core. Or stop after 30 turns.
```

## Co ewaluator sprawdza
Ostatnia wiadomość: lista plików `qameraai/*` + `php -l` „No syntax errors" dla każdego `.php` (lub cytat
sygnatur klienta + bloku grupowania/voting). End state = kod render+odczyt z API, nie żywy widok.

## Bound
30 tur — UI + nowe metody klienta + cache, bez runtime.

## Ręczny smoke test (DoD M2, poza ewaluatorem)
Zakładka „Qamera AI" widoczna na edycji produktu (PS8/9) → pokazuje zdjęcia/packshoty/sesje pogrupowane z API
→ stan packshota/sesji = voting z API → pusty stan i błąd katalogu obsłużone.
