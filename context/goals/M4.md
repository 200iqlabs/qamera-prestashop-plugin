# /goal — M4 Flow B: packshot → sesja + publikacja do galerii

> Gotowa komenda `/goal`. Świeża sesja. Kontekst: `goals.md` → M4. Ewaluator czyta TYLKO transkrypt.
> Wymaga: M3 ukończony.

## Komenda

```
/goal Flow B (packshot -> session) and gallery publishing are implemented in the qameraai module. End state: AdminQameraAjaxController gains a generate (session) action that validates the source is a session-ready packshot (an accepted generated packshot OR a direct packshot; it rejects raw photos and sessions), reads session params (preset_id, model_id, scenery_id, aspect_ratio, ai_model, custom suggestions, count 1..10), builds suggestions from product tags + specific/general category + user text, submits a job_type=photo_shoot via POST /jobs with session_config + subjects, and persists the returned order_id into ps_qamera_order; qameraai/views/js/qamera-product.js polls and renders session results; accept and reject of a session call POST /jobs/{id}/accept and /reject; on accept the session output image is imported into the product's PrestaShop images (ps_image) and the mapping is recorded in ps_qamera_import to prevent duplicate import on re-open; access helpers for ps_qamera_order and ps_qamera_import exist. The hard rule is enforced in code: a session can be created only from a packshot, never directly from a photo. PHP 7.4-compatible syntax, PrestaShop 8 and 9. Prove completion in your final message by: (1) listing every created/modified file path under qameraai/, and (2) pasting `php -l` output showing "No syntax errors detected" for each .php file (if php CLI is unavailable, state that and instead quote the generate action validation, the import-to-ps_image code, and the ps_qamera_import dedup check). Constraints: only files under qameraai/ are created or modified; prd.md, goals.md, CLAUDE.md and context/ are NOT touched; no libraries beyond PrestaShop core. Or stop after 40 turns.
```

## Co ewaluator sprawdza
Lista plików `qameraai/*` + `php -l` „No syntax errors" dla każdego `.php` (lub cytat: walidacja `generate`,
import do `ps_image`, dedup `ps_qamera_import`). End state = kod flow B + publikacja gotowy, nie żywa galeria.

## Bound
40 tur — najobszerniejszy: sesja + parametry + publikacja + dedup.

## Ręczny smoke test (DoD M4, poza ewaluatorem)
Sesja z gotowego packshota z pełnymi parametrami → próba sesji ze zdjęcia zablokowana → wyniki po pollingu,
accept/reject → zatwierdzona sesja w galerii produktu (storefront), bez duplikatów po odświeżeniu.
