# /goal — M1 Fundament modułu

> Gotowa komenda dla autonomicznej pętli `/goal` (Claude Code). Odpalaj w ŚWIEŻEJ sesji.
> Kontekst milestone: `goals.md` → M1. Ewaluator czyta TYLKO transkrypt (nie odpala PS, nie instaluje).

## Komenda

```
/goal The PrestaShop module skeleton for "qameraai" is fully implemented under the qameraai/ directory. End state: all of these files exist and are syntactically valid PHP — qameraai/qameraai.php (Module subclass whose install() creates tables ps_qamera_order and ps_qamera_import and registers Configuration keys QAMERA_API_KEY, QAMERA_DEFAULT_PRESET_ID, QAMERA_API_BASE, and whose uninstall() drops those tables and deletes those keys; getContent() renders a settings form with an API key field, the webhook/status info, calls the API client for account status+credit balance and a preset dropdown, and shows a readable error when the key is missing/invalid), qameraai/config.xml, qameraai/classes/QameraApiClient.php (X-Api-Key header, base /api/v1/plugin, methods get_me() and get_presets(), error envelope {error:{code,message_i18n}} mapped to a readable message), and the SQL/install logic for both tables. Code targets PrestaShop 8 and 9 with PHP 7.4-compatible syntax (no syntax requiring PHP 8.0+). You prove completion in your final message by: (1) listing every created file path under qameraai/, and (2) pasting the output of `php -l` for each .php file showing "No syntax errors detected" (if php CLI is unavailable, state that explicitly and instead quote the install()/uninstall() and get_me()/get_presets() function signatures with the table-create/drop and Configuration calls). Constraints: only files under qameraai/ are created or modified; prd.md, goals.md, CLAUDE.md and context/ are NOT touched; no extra libraries added beyond PrestaShop core. Or stop after 30 turns.
```

## Co ewaluator sprawdza
Ostatnia wiadomość Claude: (1) wymienia ścieżki plików `qameraai/*`, (2) wkleja `php -l` = „No syntax errors"
dla każdego `.php` (lub, gdy brak php, cytuje sygnatury `install/uninstall/get_me/get_presets` z create/drop
tabel + Configuration). End state = kod kompletny + składnia OK, nie żywa instalacja.

## Bound
30 tur — średnio-duży milestone (kilka plików, kompat PS8/9), bez integracji runtime.

## Przed odpaleniem
- `.env` z kluczem API testowym (`mk_live_...`) — patrz `.env.example`.
- Decyzja §7 PRD: `QAMERA_API_BASE` default = prod `https://qamera.ai`, override przez `Configuration`.
- Świeża sesja per goal (playbook Faza 4).

## Ręczny smoke test po pętli (DoD M1, poza zakresem ewaluatora)
Instalacja modułu na czystym PS8 i PS9 → zakładka/settings widoczne → wklejenie klucza pokazuje status konta
+ saldo z `/me` → dropdown presetów z `/presets` → zły klucz = czytelny komunikat.
