# /goal — M3 Flow A: zdjęcie → packshot (sync + polling)

> Gotowa komenda `/goal`. Świeża sesja. Kontekst: `goals.md` → M3. Ewaluator czyta TYLKO transkrypt.
> Wymaga: M2 ukończony.

## Komenda

```
/goal Flow A (product photo -> packshot, synchronous submit + polling) is implemented in the qameraai module. End state: qameraai/controllers/admin/AdminQameraAjaxController.php exists with a generatePackshot action that synchronously ensures the source asset in Qamera's catalog (upload via POST /assets/upload, register via POST /packshots, cache the asset_id on the source, dedup by sha256) and submits a job_type=packshot via POST /jobs, returning the job id; qameraai/classes/QameraApiClient.php gains upload_asset(), register_packshot(), submit_job() with Idempotency-Key, and get_job(); qameraai/views/js/qamera-product.js polls GET /jobs/{id} (interval 3s, max 5min) and renders the resulting packshot without page reload; accept and reject of a packshot call POST /jobs/{id}/accept and POST /jobs/{id}/reject; the code waits for source image analysis_status='described' before submitting and clamps the variant count to 1..10; API errors (missing key, no credits, 4xx/5xx, network) surface a readable message immediately in the sync response. PHP 7.4-compatible syntax, PrestaShop 8 and 9. Prove completion in your final message by: (1) listing every created/modified file path under qameraai/, and (2) pasting `php -l` output showing "No syntax errors detected" for each .php file (if php CLI is unavailable, state that and instead quote the generatePackshot action body and the new QameraApiClient method signatures). Constraints: only files under qameraai/ are created or modified; prd.md, goals.md, CLAUDE.md and context/ are NOT touched; no libraries beyond PrestaShop core. Or stop after 30 turns.
```

## Co ewaluator sprawdza
Lista plików `qameraai/*` + `php -l` „No syntax errors" dla każdego `.php` (lub cytat akcji `generatePackshot`
+ sygnatur klienta). End state = kod flow A gotowy, nie żywa generacja.

## Bound
30 tur — kontroler AJAX + ensure-source + polling.

## Ręczny smoke test (DoD M3, poza ewaluatorem)
Klik „Generuj packshot" → spinner → packshot bez przeładowania → accept/reject działa → błąd czytelny od razu
→ count 1–10 działa.
