# /goal — M5 Hardening + pakiet dystrybucyjny

> Gotowa komenda `/goal`. Świeża sesja. Kontekst: `goals.md` → M5. Ewaluator czyta TYLKO transkrypt.
> Wymaga: M4 ukończony.

## Komenda

```
/goal The qameraai module is hardened and packaged for distribution. End state: every API call path maps errors (missing/invalid key, insufficient credits, 429 rate limit, 4xx/5xx, network failure) to a readable admin message with no fatal error or white screen; PL and EN translations exist for all user-facing strings (PrestaShop translation files under qameraai/translations/); qameraai/README.md documents installation, where to obtain the API key, configuration, and the end-to-end flow; a distributable ZIP of the module is built. PHP 7.4-compatible syntax across all files, PrestaShop 8 and 9. Prove completion in your final message by: (1) listing every created/modified file path under qameraai/, (2) pasting `php -l` output showing "No syntax errors detected" for EVERY .php file in qameraai/, (3) confirming the README.md section headings and listing the PL and EN translation files, and (4) pasting the output of the ZIP build (e.g. `zip -r qameraai.zip qameraai` followed by the first lines of `unzip -l qameraai.zip`) — if any tool is unavailable, state that explicitly. Constraints: only files under qameraai/ (plus the build ZIP) are created or modified; prd.md, goals.md, CLAUDE.md and context/ are NOT touched. Or stop after 25 turns. Note: a live install plus full Core Flow smoke test on PrestaShop 8 and 9 is a manual step outside this loop.
```

## Co ewaluator sprawdza
Lista plików + `php -l` „No syntax errors" dla KAŻDEGO `.php` + nagłówki README + lista plików tłumaczeń PL/EN
+ output budowy ZIP (`unzip -l`). End state = hardening+i18n+README+ZIP, nie żywa instalacja.

## Bound
25 tur — przekrojowe poprawki + docs + pakowanie.

## Ręczny smoke test (DoD M5 §8 prd.md, poza ewaluatorem)
Instalacja ZIP przez Menedżer modułów → cały Core Flow §3 end-to-end na PS8 i PS9 (realne konto) →
wszystkie kryteria §3 i DoD §8 ☑.
